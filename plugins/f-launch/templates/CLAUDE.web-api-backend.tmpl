# {{NAME}}

> 类型:web-api-backend · 由 f-launch skill 创建于 {{DATE}}
> 参考:[<project-name> 技术选型报告](https://<your-tenant>.feishu.cn/docx/<your-doc-token>)

## 目标

<!-- 一句话目标:这个 API 服务给谁用,核心业务是什么 -->

## 技术栈(2026 主流平衡组合)

| 组件 | 选型 | 版本要求 |
|------|------|----------|
| 运行时 | Node.js | 22 LTS |
| 语言 | TypeScript | strict mode |
| Web 框架 | Hono | ^4.6 |
| ORM | Drizzle | ^0.45 |
| 数据库(开发) | SQLite | WAL 模式 |
| 数据库(生产) | PostgreSQL | 16+ |
| 数据验证 | Zod | ^3.23 |
| 测试 | Vitest | ^1.x |
| 鉴权 | better-auth | ^1.6 |
| 部署(MVP) | Render | free tier |
| 部署(运营) | Railway | $5/mo |

## 目录结构

```
{{NAME}}/
├── src/
│   ├── index.ts           # 入口
│   ├── app.ts             # Hono app
│   ├── routes/
│   │   ├── users.ts
│   │   ├── posts.ts
│   │   └── auth.ts
│   ├── db/
│   │   ├── schema.ts      # Drizzle schema
│   │   ├── client.ts
│   │   └── migrations/
│   ├── lib/
│   │   ├── auth.ts
│   │   ├── validate.ts
│   │   └── errors.ts
│   └── types/
├── tests/
│   ├── unit/
│   └── integration/
├── docs/
│   ├── plan.md
│   ├── stack.md
│   └── api.md
├── .env.example
├── package.json
├── tsconfig.json
├── drizzle.config.ts
├── vitest.config.ts
└── ...
```

## 开发

```bash
# 安装依赖
npm install

# 数据库初始化(开发)
npm run db:push

# 启动开发服务器
npm run dev

# 测试
npm test
```

## 环境变量

```bash
# .env(不要提交)
DATABASE_URL=file:./dev.db     # SQLite
# 或 PostgreSQL
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname
BETTER_AUTH_SECRET=<random-32-chars>
PORT=3000
NODE_ENV=development
```

## 部署

### MVP(零成本)
- Render free tier:web service + Postgres 90 天

### 运营(稳定)
- Railway $5/mo:自动检测 runtime
- 详见 `docs/deploy.md`

## 学习路径(8 周)

| 周 | 任务 |
|----|------|
| 1 | Node 22 + TS strict + Hono + Drizzle + SQLite 本地 |
| 2 | CRUD: users / posts / tags,DTO 用 Zod |
| 3 | better-auth: email+password + JWT + RBAC |
| 4 | 测试: Vitest + testcontainers (Postgres) |
| 5 | 部署 Render: web service + Postgres + 自动迁移 |
| 6 | 加 Drizzle Postgres 适配器,迁移到 Postgres |
| 7 | OpenAPI (hono-openapi)+ rate limit + helmet |
| 8 | 复盘:压测、监控(Sentry/Pino)、备份、CI |

## 风险点

1. Drizzle 生态小 — 复杂查询自己写 SQL
2. better-auth v1.6 新 — 关注 changelog,准备 NextAuth fallback
3. Hono 跨 runtime 优势 MVP 阶段用不上 — 实测压测
4. SQLite → Postgres 迁移行为差异 — 第 6 周专门做迁移 + 集成测试
5. better-auth + Drizzle 集成文档细节 — 第 3 周实测 demo

## 关联资源

- Hono:https://hono.dev/
- Drizzle:https://orm.drizzle.team/
- better-auth:https://www.better-auth.com/
- Vitest:https://vitest.dev/
- f-launch 类型模板:`~/.claude/skills/f-launch/references/web-api-backend.md`
